import { Rings } from "react-loader-spinner";
import "./Spinner.css";

export default function Spinner() {
    return (
        <div className="Spinner">
            <Rings color="blue" />
        </div>
    );
}
